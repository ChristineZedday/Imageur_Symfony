{% extends 'base.html.twig' %}

{% block title %}Javascript{% endblock %}

{% block content %}
    <h1>Javascript</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ javascript.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ javascript.nom }}</td>
            </tr>
        </tbody>
    </table>

  <p>  <a href="{{ path('javascript_index') }}">Retour à la liste</a></p>

  <p>  <a href="{{ path('javascript_edit', {'id': javascript.id}) }}">Modifier le fichier</a></p>
   <p>  <a href="{{ path('javascript_articles', {'id': javascript.id}) }}">Appliquer ce javascript à tous les articles</a></p>

    {{ include('javascript/_delete_form.html.twig') }}
 
{% endblock %}
