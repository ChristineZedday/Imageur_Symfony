{% extends 'base.html.twig' %}

{% block title %}Javascript{% endblock %}

{% block body %}
    <h1>Javascript</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ javascript.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ javascript.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('javascript_index') }}">back to list</a>

    <a href="{{ path('javascript_edit', {'id': javascript.id}) }}">edit</a>

    {{ include('javascript/_delete_form.html.twig') }}
{% endblock %}
