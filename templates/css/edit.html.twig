{% extends 'base.html.twig' %}

{% block title %}Edit CSS{% endblock %}

{% block content %}
    <h1>Modifier le fichier CSS</h1>

    {{ include('css/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

   <p> <a href="{{ path('css_index') }}">Retour à la liste</a></p>
    <p> <a href="{{ path('css_show', {id:css.id}) }}">Voir</a></p>

    {{ include('css/_delete_form.html.twig') }}
{% endblock %}
