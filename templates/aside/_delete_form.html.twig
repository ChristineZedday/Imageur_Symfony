<form method="post" action="{{ path('aside_delete', {'id': aside.id}) }}" onsubmit="return confirm('Vous voulez vraiment le supprimer?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aside.id) }}">
    <button class="btn">Supprimer</button>
</form>
