{% extends 'base.html.twig' %}

{% block title %}Edit adress{% endblock %}

{% block body %}
{{ parent() }}
<article>
    <h1>Modifier les adresses</h1>

    {{ include('adress/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('adress_index') }}">Retour à la liste</a>

    {{ include('adress/_delete_form.html.twig') }}
    </article>
{% endblock %}
