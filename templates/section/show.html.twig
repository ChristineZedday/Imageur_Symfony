{% extends 'base.html.twig' %}

{% block title %}Section{% endblock %}

{% block body %}
    <h1>Section</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ section.id }}</td>
            </tr>
              <tr>
                <th>Article</th>
                <td>{{ section.article.titre }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ section.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ section.Contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('section_index') }}">back to list</a>

    <a href="{{ path('section_edit', {'id': section.id}) }}">edit</a>

    {{ include('section/_delete_form.html.twig') }}
{% endblock %}
