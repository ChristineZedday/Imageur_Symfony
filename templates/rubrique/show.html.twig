{% extends 'base.html.twig' %}

{% block title %}Rubrique{% endblock %}

{% block body %}
<article>
    <h1>Rubrique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rubrique.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ rubrique.nom }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ rubrique.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rubrique_index') }}">Retour à la liste</a>

    <a href="{{ path('rubrique_edit', {'id': rubrique.id}) }}">Modifier</a>

    {{ include('rubrique/_delete_form.html.twig') }}
    </article>
{% endblock %}
