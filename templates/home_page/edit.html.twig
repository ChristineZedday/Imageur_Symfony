{% extends 'base.html.twig' %}

{% block title %}Edit HomePage{% endblock %}

{% block body %}
    {{ parent() }}
<article>
    <h1>Modifier la page d'accueil</h1>

    {{ include('home_page/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('home_page_index') }}">back to list</a>

    {{ include('home_page/_delete_form.html.twig') }}
    </article>
{% endblock %}
